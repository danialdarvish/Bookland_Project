@page
@using DiscountManagement.Configuration.Permissions
@model ServiceHost.Areas.Administration.Pages.Discounts.ColleagueDiscounts.IndexModel
@{
    Layout = "Shared/_AdminLayout";
    ViewData["title"] = "مدیریت تخفیفات همکاران";
}
<div class="row">
    <div class="col-sm-12">
        <h4 class="page-title pull-right">@ViewData["title"]</h4>
        <p class="pull-left">
            <a permission="@DiscountPermissions.DefineColleagueDiscounts" class="btn btn-success btn-lg" href="#showmodal=@Url.Page("./Index", "Create")">تعریف تخفیف مشتری</a>
        </p>
    </div>
</div>

<div class="row" permission="@DiscountPermissions.SearchColleagueDiscounts">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">جستجو در تخفیفات همکاران</h3>
            </div>
            <div class="panel-body">
                <form class="form-inline" role="form" method="get">
                    <div class="form-group">
                        <label class="sr-only" asp-for="SearchModel.BookId"></label>
                        <select class="form-control" asp-for="SearchModel.BookId" asp-items="@Model.Books">
                            <option value="0">محصول...</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-success waves-effect waves-light m-l-10">جستجو</button>
                    <a class="btn btn-primary waves-effect waves-light m-l-10" asp-page="./Index">مشاهده همه</a>
                </form>
            </div>
        </div>
    </div>
</div>

<div class="row" permission="@DiscountPermissions.ListColleagueDiscounts">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">لیست تخفیفات همکاران (@Model.ColleagueDiscounts.Count)</h3>
                <h5 class="text-success">@Model.Message</h5>
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-md-12 col-sm-12 col-xs-12">
                        <table id="datatable" class="table table-striped table-bordered">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>محصول</th>
                                    <th>درصد تخفیف</th>
                                    <th>تاریخ تولید</th>
                                    <th>عملیات</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.ColleagueDiscounts)
                                {
                                    <tr>
                                        <td>@item.Id</td>
                                        <td>@item.Book</td>
                                        <td>@item.DiscountRate%</td>
                                        <td>@item.CreationDate</td>
                                        <td>
                                            <a class="btn btn-warning btn-custom waves-effect waves-light m-b-5" href="#showmodal=@Url.Page("./Index", "Edit", new {id = item.Id})">
                                                <i class="fa fa-edit"></i> ویرایش
                                            </a>
                                            @if (item.IsRemoved)
                                            {
                                                <a permission="@DiscountPermissions.RestoreColleagueDiscounts" asp-page="" asp-page-handler="Restore" asp-route-id="@item.Id"
                                                   class="btn btn-success btn-custom waves-effect waves-light m-b-5">فعال سازی</a>
                                            }
                                            else
                                            {
                                                <a permission="@DiscountPermissions.RemoveColleagueDiscounts" asp-page="" asp-page-handler="Remove" asp-route-id="@item.Id"
                                                   class="btn btn-danger btn-custom waves-effect waves-light m-b-5">غیر فعال</a>
                                            }
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/adminTheme/assets/datatables/jquery.dataTables.min.js"></script>
    <script src="~/adminTheme/assets/datatables/dataTables.bootstrap.js"></script>

    <script type="text/javascript">
        $(document).ready(function () {
            $('#datatable').dataTable();
        });
    </script>
}